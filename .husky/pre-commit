#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run type check
echo "📝 Type checking..."
pnpm type-check || {
  echo "❌ TypeScript type check failed. Please fix the errors above."
  exit 1
}

# Run lint-staged (includes lint and format)
echo "🧹 Linting and formatting staged files..."
pnpm lint-staged || {
  echo "❌ Linting or formatting failed. Please fix the errors above."
  exit 1
}

echo "✅ All pre-commit checks passed!"
